## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues (uses `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 5-role triage vocabulary mapped to GitHub issue labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context domain layout (`CONTEXT.md` at root, `docs/adr/`). See `docs/agents/domain.md`.
